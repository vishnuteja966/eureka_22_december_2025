import { SalutationPipe } from './salutation-pipe';

describe('SalutationPipe', () => {
  it('should create an instance', () => {
    const directive = new SalutationPipe();
    expect(directive).toBeTruthy();
  });
});
