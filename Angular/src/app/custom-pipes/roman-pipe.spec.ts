import { RomanPipe } from './roman-pipe';

describe('RomanPipe', () => {
  it('should create an instance', () => {
    const directive = new RomanPipe();
    expect(directive).toBeTruthy();
  });
});
