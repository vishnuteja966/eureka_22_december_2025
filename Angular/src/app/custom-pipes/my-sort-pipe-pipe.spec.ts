import { MySortPipePipe } from './my-sort-pipe-pipe';

describe('MySortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MySortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
