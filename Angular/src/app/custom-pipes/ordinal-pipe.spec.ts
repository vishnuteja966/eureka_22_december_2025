import { OrdinalPipe } from './ordinal-pipe';

describe('OrdinalPipe', () => {
  it('should create an instance', () => {
    const directive = new OrdinalPipe();
    expect(directive).toBeTruthy();
  });
});
