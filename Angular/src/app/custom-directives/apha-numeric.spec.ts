import { AphaNumeric } from './apha-numeric';

describe('AphaNumeric', () => {
  it('should create an instance', () => {
    const directive = new AphaNumeric();
    expect(directive).toBeTruthy();
  });
});
