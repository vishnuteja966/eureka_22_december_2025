import { Disablecopypaste } from './disablecopypaste';

describe('Disablecopypaste', () => {
  it('should create an instance', () => {
    const directive = new Disablecopypaste();
    expect(directive).toBeTruthy();
  });
});

